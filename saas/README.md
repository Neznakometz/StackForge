# saas — domain set

Backend/web services on top of the StackForge core.

**Skills:** `api-design` · `background-jobs` · `multitenancy`.
**From the core:** `contracts`, `task-loop`, `cross-model-review`, `security-auditor`.
**Stack knowledge (via `/init`):** knowledge packs `php-laravel` / `python-django` / `node-nestjs`, `data-sql`, `data-cache`, `data-search-olap`.

**External skills (optional, installed separately):**
- `postgres-best-practices` (Supabase) — indexes, RLS, zero-downtime migrations.
- `mcp-builder` (Anthropic) — build an MCP server for an internal API.
